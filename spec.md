# Agent-readable SOP notation (v0.1)

This document describes a lightweight notation for expressing standard operating procedures (SOPs in explicit, agent-readable form.

The intent is to remove ambiguity by making inputs, limits, decision logic, allowed actions, and escalation rules explicit, while remaining suitable for human review and sign-off.

This is a working note, not a complete specification.
